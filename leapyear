# This program determines if an inputted year is a leap year
year = int(input("Which year do you want to check? "))
if year % 4 == 0:
  if year % 100 == 0:
    if year % 400 == 0:
      print(f"{year} is a leap year".format(year))
    else:
      print(f"{year} is not a leap year".format(year))
  else:
    print(f"{year} is not a leap year".format(year))
else:
  print(f"{year} is not a leap year".format(year))
